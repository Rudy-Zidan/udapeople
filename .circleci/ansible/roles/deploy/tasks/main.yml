---

- name: Creates App directory
  file:
    path: ~/app
    state: directory

- name: Upload dist to app directory
  become: true
  copy:
    src: "files/dist"
    dest: "~/app"

- name: Install dependencies
  become: true
  shell: |
    cd ~/app/dist
    npm i

- name: Start backend
  become: true
  command: pm2 start ~/app/dist/main.js --name backend -f